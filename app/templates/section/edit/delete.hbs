{{page-title "Delete"}}
<div class="ui segment">
  <form class="ui form">
    <h4 class="ui dividing header">Supprimer un produit : {{@model.name}}</h4>
    <LinkTo class="ui button" @route="section">Annuler</LinkTo>
    <button class="ui button"{{action 'deleteProduct' @model}}>Valider suppression</button>
  </form>
</div>
{{outlet}}